apiVersion: apps/v1
kind: Deployment
metadata:
  name: spiffe-demo-backend
  namespace: sandbox
  labels:
    app: spiffe-demo-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: spiffe-demo-backend
  template:
    metadata:
      labels:
        app: spiffe-demo-backend
    spec:
      serviceAccountName: spiffe-demo-backend
      containers:
        - name: backend
          image: spiffe-demo-backend:latest
          imagePullPolicy: Never
          ports:
            - containerPort: 8080
          volumeMounts:
            - name: spiffe-csi
              mountPath: /var/run/secrets/spiffe.io
              readOnly: true
          env:
            - name: NODE_ENV
              value: "production"
            - name: CERBOS_HOST
              value: "cerbos-service.authorization.svc.cluster.local:3593"
      volumes:
        - name: spiffe-csi
          csi:
            driver: spiffe.csi.cert-manager.io
            readOnly: true
            volumeAttributes:
              spiffe.csi.cert-manager.io/fs-group: "1000"
---
apiVersion: v1
kind: Service
metadata:
  name: spiffe-demo-backend-service
  namespace: sandbox
spec:
  selector:
    app: spiffe-demo-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
---
apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: spiffe-demo-backend
  namespace: sandbox
spec:
  hosts:
    - spiffe-backend.local
  gateways:
    - spiffe-demo-gateway
  http:
    - route:
        - destination:
            host: spiffe-demo-backend-service.sandbox.svc.cluster.local
            port:
              number: 80
